#ifndef SINGLE_THREAD_H
#define SINGLE_THREAD_H

#include "args.h"

/**
* @author Arnaud and Gabriel
*
* @brief Combines all the functions described in the headers and solves the k-means problem by applying the Lloyd's algorithm. It's the single threaded version of the program.
*
* @see args.h
*/
int singleThread(args_t* program_arguments);

#endif //SINGLE_THREAD_H